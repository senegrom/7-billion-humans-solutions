-- Novel Year 47 speed tie-break: remove final standalone tautological delay

if w == worker:
	if e == nothing:
		if nw == nw:
			if nw == nw:
				if nw == nw:
					if nw == nw:
						if nw == nw:
							if nw == nw:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
		tell w morning
	endif
	listenfor morning
endif
tell e morning
