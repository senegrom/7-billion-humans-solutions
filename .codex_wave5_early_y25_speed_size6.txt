-- Wave 5 candidate: Year 25 speed 9 / queued 8 -> 6.
-- The holding guard handles pickup races and retries failed gives.

mem1 = nearest shredder
a:
mem2 = nearest datacube
pickup mem2
if myitem == datacube:
	giveto mem1
endif
jump a

